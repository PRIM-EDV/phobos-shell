window.__env = window.__env || {};

// Auth
window.__env.phobosAuthUrl = '${PHOBOS_AUTH_URL}';

// Maptool
window.__env.trxServerHostname = '${TRX_SERVER_HOSTNAME}';
window.__env.trxServerPort = '${TRX_SERVER_PORT}';

// LSX
window.__env.lsxServerHostname = '${LSX_SERVER_HOSTNAME}';
window.__env.lsxServerPort = '${LSX_SERVER_PORT}';