window.__env = window.__env || {};

window.__env.PHOBOS_AUTH_URL = '${PHOBOS_AUTH_URL}';

window.__env.LSX_SERVER_HOSTNAME = '${LSX_SERVER_HOSTNAME}';
window.__env.LSX_SERVER_PORT = '${LSX_SERVER_PORT}';